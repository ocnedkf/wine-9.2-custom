## BUILD
The build methods for 32-bit mode, 64-bit mode, and Wow64 mode can be referenced from here:
<br>
https://github.com/Kron4ek/Wine-Builds/blob/master/build_wine.sh
<br>
NOTE:It’s best to build inside a sandbox created with chroot or proot; otherwise the system may be damaged while dependencies are being installed—for example, Wi-Fi could stop working.